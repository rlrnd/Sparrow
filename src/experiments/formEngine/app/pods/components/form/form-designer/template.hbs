{{#component componentClass data=data caption=def.caption editorType=def.editorType bind=def.bind }}
    {{#each childComponents as |child|}}
      {{form/form-designer dropped=dropped parent=this parent=thisDesigner def=child}}
    {{/each}}
{{/component}}
